# CLAUDE.md

本项目的 AI 行为规范定义在 AGENTS.md，技术栈与架构定义在 openspec/project.md。
涉及 OpenSpec change / spec / archive 时，使用当前 agent 已安装的 OpenSpec 官方 skills / commands，并读取 openspec/config.yaml。
请先阅读这些规则源再开始对应工作。

## Quick Reference

- 主开发流程：`/design` → `/prd` → `/change-propose` → dispatch runner 自动执行 → `/change-review`
- 五层架构：`design/inputs/` → `design/modules/` → `product/backlog.md` → `product/prd/` → `openspec/changes/`
- 全局蓝图：`design/roadmap.md`（AUTO 段从 backlog + modules 全量重渲染：ARCHITECTURE / DEPENDENCIES / PROGRESS）
- 分形文档：每个目录维护 `_DIR.md`，关键文件维护 `@input/@output/@pos`
- 实现顺序见 `openspec/project.md`
- 禁止项见 `AGENTS.md ## Do NOT`
- 执行日志：`.logs/{module-designer,prd,propose,dispatch,review}/` — STOP/WARN 问题和路径偏离自动记录

## Commands

本框架的 5 个自动化 skill（主路径）：

- `/design` — 从 design-inputs 建立/扩展模块 + 拆 idea 到 backlog（module-designer）
  - `/design review M-NNN` — 追加 backlog 到已有模块 / 调整模块边界
- `/prd B-NNN` — 从 backlog 条目生成 PRD（prd-writer，要求 backlog 模块列已填）
- `/change-propose` — 扫描 approved PRD 生成四件套 + Draft PR（可人工触发或由 runner 定期触发）
- `change-dispatch` — 由 dispatch runner 自动执行任务组（Codex Automation / Claude Code `/loop` / cron / GH Actions 任选其一；不作为人工触发的 slash command）
- `/change-review` — 审查、合并、verify、归档（可人工触发或由 runner 定期触发）

OpenSpec 官方 CLI / slash commands 只作为底层能力接入，不作为本框架主路径。

## Parallel Development

本项目支持基于 git worktree 的多路并行开发，详见 AGENTS.md。
并行开发时 `.worktrees/` 目录已在 `.gitignore` 中排除。

## Backlog YAML Schema（"模块"列必填）

`product/backlog.md` 每一行必须在"模块"列填写 `M-NNN`（由 `module-designer` 分配）。
历史遗留行可以保持 `—`；自动化 skill 对这些行会跳过模块/roadmap 同步。

## PRD YAML Schema

PRD 文件头必须包含以下字段：

```yaml
id: PRD-NNN
type: feature | bug | chore | hotfix        # 与 backlog.type 必须一致
status: draft | reviewing | approved | superseded
backlog-ref: B-NNN
module-ref: M-NNN                            # 必填，= backlog "模块"列
design-inputs:                               # 必填，至少含模块文档
  - design/modules/M-NNN-<slug>.md
  - design/inputs/brainstorming/<file>.md    # 按对话需要追加
depends-on: [B-xxx]                          # 前置 backlog 依赖，空则 []
change-id: kebab-case                        # 预定义（bug→fix-* / chore→chore-* / hotfix→hotfix-*）
```

`change-propose` 在 Phase 0 筛选时会强校验 `module-ref` / `design-inputs` / 模块文档存在性，缺字段/缺文件则跳过并写日志。
