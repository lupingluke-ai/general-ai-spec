---
id: PRD-NNN
type: feature             # Full PRD 仅用于 feature 类型（bug/chore/hotfix 请使用 prd-lite.md.tmpl）
backlog-ref: B-NNN
module-ref: M-NNN         # 必填；与 backlog 条目的"模块"列一致；跨模块的需求必须拆
status: draft
created: YYYY-MM-DD
depends-on: []            # 前置 backlog 依赖，如 [B-001, B-002]，无则 []
change-id: kebab-case     # 由 prd-writer 按类型前缀规则生成（feature 无前缀 / bug fix- / chore chore- / hotfix hotfix-），全局唯一，禁止保留占位符
design-inputs:            # 必填；prd-writer 必读这些路径作为产品语境
  - design/modules/M-NNN-<slug>.md          # 模块设计文档（必引）
  # - design/inputs/brainstorming/<name>.md  # 追加的原始输入（可选）
  # - design/inputs/figma/<name>.md          # 追加的原始输入（可选）
---

# <功能名称>

## 背景与问题

为什么做这个功能？解决谁的什么问题？当前体验的痛点是什么？

## 目标用户

主要用户画像，核心使用场景。

## 用户故事

- 作为 <角色>，我想 <操作>，以便 <目的>
- 作为 <角色>，我想 <操作>，以便 <目的>

## 功能需求

### 核心功能（Must Have）

- FR-1: <需求描述>
- FR-2: <需求描述>

### 增强功能（Nice to Have）

- FR-N: <需求描述>

### 明确不做（Out of Scope）

- <明确排除的功能或场景>

## 非功能需求

- **性能**：<响应时间、加载速度等>
- **安全**：<数据保护、权限控制等>
- **兼容性**：<浏览器、设备等>
- **无障碍**：<可访问性要求>
- **离线**：<离线支持策略>

## 验收标准

- AC-1: <可验证的验收条件>
- AC-2: <可验证的验收条件>

## 依赖与风险

| 依赖/风险 | 影响 | 应对策略 |
|-----------|------|----------|
| <描述> | <影响范围> | <应对方式> |

## 成功指标

- <可量化的衡量方式>
